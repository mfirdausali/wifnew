{
  "name": "@multirole-auth/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for multi-role auth system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}